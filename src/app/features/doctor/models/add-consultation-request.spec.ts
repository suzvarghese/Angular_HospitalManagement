import { AddConsultationRequest } from './add-consultation-request';

describe('AddConsultationRequest', () => {
  it('should create an instance', () => {
    expect(new AddConsultationRequest()).toBeTruthy();
  });
});
