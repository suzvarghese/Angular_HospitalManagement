import { AddConsultationResult } from './add-consultation-result';

describe('AddConsultationResult', () => {
  it('should create an instance', () => {
    expect(new AddConsultationResult()).toBeTruthy();
  });
});
