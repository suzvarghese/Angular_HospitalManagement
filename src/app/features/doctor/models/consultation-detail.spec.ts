import { ConsultationDetail } from './consultation-detail';

describe('ConsultationDetail', () => {
  it('should create an instance', () => {
    expect(new ConsultationDetail()).toBeTruthy();
  });
});
