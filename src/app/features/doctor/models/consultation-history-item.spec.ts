import { ConsultationHistoryItem } from './consultation-history-item';

describe('ConsultationHistoryItem', () => {
  it('should create an instance', () => {
    expect(new ConsultationHistoryItem()).toBeTruthy();
  });
});
